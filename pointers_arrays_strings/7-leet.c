#include "main.h"
/**
 * leet - encode string into 1337
 * @s: string to be encoded
 *
 * Return: string encoded
 */

char *leet(char *s)
{
	int i, j;

	for (i = 0; s[i] != '\0'; i++)
	{
		for (j = 0; s[j] != '\0'; j++)
		{
		}
	}
	return (s);
}
